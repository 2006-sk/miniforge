# Test assets

Place a clear product-label photo here as:

```
test-assets/sample-label.jpg
```

Use a well-lit, close-up photo of a food, cosmetic, or medicine ingredient list so the vision step can extract ingredients.

If the file is missing, `scripts/smoke.js` writes a 1×1 placeholder JPEG (vision will report `readability: "poor"`).
